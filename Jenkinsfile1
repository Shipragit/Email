
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build is complete"
            }
        }
    }
    post {
        always {
            emailext(
                subject: "Build Status: ${currentBuild.result}",
                body: '''<html>
                            <body>
                                <p>Build Status: ${currentBuild.result}</p>
                                <p>Job Name: ${JOB_NAME}-${BUILD_NUMBER}</p>
                                <p>Build URL: ${BUILD_URL}</p>
                            </body>
                        </html>''',
                to: 'shiva291291@gmail.com',
                from: 'shiva291291@gmail.com',
                replyTo: 'shiva291291@gmail.com',
                mimeType: 'text/html'
            )
        }
    }
}
